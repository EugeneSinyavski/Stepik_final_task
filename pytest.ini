[pytest]
markers =
    login_guest: marker for guest on the main page
    need_review: marker for tests that should be reviewed
log_format = %(asctime)s [%(levelname)s] %(name)s %(message)s
log_date_format = %Y-%m-%d %H:%M:%S